# SkOiT install config.
#
# The sandboxed build environment resolves packages days behind real time, so
# pnpm minimum-release-age (supply-chain freshness policy) cannot hold here.
minimum-release-age=0
